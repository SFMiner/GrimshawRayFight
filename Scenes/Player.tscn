; Player scene representing a single student.  This scene uses a
; CharacterBody2D for movement and a ColorRect as a simple visual
; placeholder.  An AnimationPlayer is provided for future sprite
; animations; for now it controls basic colour changes or scale
; transitions.

[gd_scene load_steps=4 format=3]

[ext_resource type="Script" path="res://Scripts/Player.gd" id=1]

[node name="Player" type="CharacterBody2D"]
script = ExtResource( 1 )

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]

[node name="Visual" type="ColorRect" parent="."]
; The default colour and size can be overridden by the script.  The
; colour is white so that players can be tinted via code.
color = Color(1, 1, 1, 1)
size = Vector2(32, 32)

[node name="AnimationPlayer" type="AnimationPlayer" parent="."]
; No animations are defined in this template.  Placeholder for user
; supplied animations.