; Scene definition for a power‑up that restores the scale of any
; characters within its area of effect.  A PowerUp is a simple
; Node2D with an Area2D child and a ColorRect visual indicator.

[gd_scene load_steps=5 format=3]

[ext_resource type="Script" path="res://Scripts/PowerUp.gd" id=1]

[node name="PowerUp" type="Node2D"]
script = ExtResource( 1 )

[node name="Area" type="Area2D" parent="."]
; The Area2D determines which characters are within the power‑up’s
; radius.  The collision shape is created by the script when
; spawning.

[node name="CollisionShape2D" type="CollisionShape2D" parent="Area"]

[node name="Visual" type="ColorRect" parent="."]
; Visual representation of the power‑up.  The size will be scaled
; according to the configured radius in PowerUp.gd.
color = Color(0.3, 0.8, 0.3, 0.5)
size = Vector2(64, 64)

[node name="AnimationPlayer" type="AnimationPlayer" parent="."]